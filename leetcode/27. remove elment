class Solution :
    def removeElement (self, nums: List[int], val:int) -> int:
        k=0
        for i im range(len(nums)):
        if nums[i] != val:
        mums[k] = nums[i]
        k +=1
    return k
